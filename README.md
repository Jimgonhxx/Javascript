Here you can find testing script.js).
App.js(css) for website structure with javascrip functions (with Math)
(if) Javascript functions, with (if/else/else if) 
If A(number) is bigger than B(number) if its equal, or its smaller,if it is then how much.
And other which contain mathematical queries, for example to calculate the circumference of a circle if we give the radius (zadanie.js/html)
